{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

    {{ form(form) }}


    <table class="table">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Nom</th>
            <th scope="col">Action</th>
        </tr>
        </thead>
        <tbody>
        {% for matiere in matieres %}
            <tr>



                <th scope="row">{{ matiere.id }}</th>
                <td>{{ matiere.name }}</td>
                <td><a href="{{ path('matiereEdit', {'id': matiere.id}) }}" class="btn btn-danger">
                        modifier
                    </a><a href="{{ path('deleteMatiere', {'id': matiere.id}) }}" class="btn btn-danger">
                        supprimer
                    </a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
